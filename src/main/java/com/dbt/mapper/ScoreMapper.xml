<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dbt.mapper.ScoreMapper">

    <insert id="addScore">
        insert into t_score values (#{studentId},#{classPerformance},#{finalExam},#{sumScore})
    </insert>
</mapper>